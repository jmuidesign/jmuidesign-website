/** @type {import('tailwindcss').Config} */
export default {
  theme: {
    extend: {
      colors: {
        dark: '#080808',
      },
      fontFamily: {
        sans: ['DM Sans', 'sans-serif'],
        serif: ['Clash Display', 'serif'],
      },
      maxWidth: {
        container: '82rem',
      },
    },
  },
}
